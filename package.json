{
  "name": "interlinear-bible",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "env NODE_ENV=production npx eleventy --pathprefix 'bereia'",
    "dev": "NODE_OPTIONS=--max-old-space-size=4096 eleventy --serve",
    "serve": "node server.js",
    "dev:full": "NODE_OPTIONS=--max-old-space-size=4096 eleventy --serve & node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "pagefind": "^1.4.0"
  },
  "dependencies": {
    "consolelogger": "^1.0.4",
    "express": "^5.2.1",
    "tooltipster": "^4.2.8"
  }
}
