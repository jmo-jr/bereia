{
  "name": "interlinear-bible",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "env NODE_ENV=production npx eleventy --pathprefix 'nt-interlinear'",
		"dev": "NODE_OPTIONS=--max-old-space-size=4096 eleventy --serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "pagefind": "^1.4.0"
  },
  "dependencies": {
    "consolelogger": "^1.0.4"
  }
}
